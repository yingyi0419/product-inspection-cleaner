import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// 使用相對路徑，部署到 GitHub Pages 專案網址或自訂網域都能載入靜態資源。
export default defineConfig({
  plugins: [react()],
  base: "./",
  build: {
    outDir: "dist",
    sourcemap: false,
  },
});
